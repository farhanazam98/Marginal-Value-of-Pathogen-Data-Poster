# layout-test/ — throwaway

MOCK figures for checking whether two stacked line graphs fit the Results block.
**Fake data, watermarked "NOT REAL DATA". Not pipeline output.**
Delete this whole folder before the poster is finalized. Never copy these into `figures/`.

Generated by `make_mocks.py` (matplotlib), sized to the values derived from
`main.tex` + tikzposter internals:

| file | figsize (in) | ~mm | represents |
|---|---|---|---|
| `mock_slot_A.pdf` / `.png` | 18.55 x 4.65 | 471 x 118 | one graph, even-split of the 28 cm slot (~4:1 panel) |
| `mock_slot_B.pdf` / `.png` | 18.55 x 4.65 | 471 x 118 | the other graph, same size |
| `mock_results_stacked.pdf` / `.png` | 18.55 x 9.30 | 471 x 236 | both panels in ONE file, drop in at `width=\linewidth` |
| `mock_reference_aspect.pdf` / `.png` | 18.55 x 23.2 | 471 x 590 | same two graphs at the reference PNGs' 1.6:1 aspect — ~2x the slot, shown to fail |

PNGs are 300 dpi at placed size (5565 px wide). PDFs are vector — prefer these.

## Test snippet — paste into the Results block of `main.tex`, compile on Overleaf

Option 1 — two separate figures (matches how you'd ship two real plots):

```latex
\column{0.62}
\block{4. Results \ \textcolor{red}{[PRELIMINARY]}}{%
  \begin{tikzfigure}[MOCK binding curve -- layout test only.]
    \includegraphics[width=\linewidth]{layout-test/mock_slot_A.pdf}
  \end{tikzfigure}
  \vspace{6mm}
  \begin{tikzfigure}[MOCK expression curve -- layout test only.]
    \includegraphics[width=\linewidth]{layout-test/mock_slot_B.pdf}
  \end{tikzfigure}
}
```

Option 2 — one stacked file:

```latex
\column{0.62}
\block{4. Results \ \textcolor{red}{[PRELIMINARY]}}{%
  \begin{tikzfigure}[MOCK stacked curves -- layout test only.]
    \includegraphics[width=\linewidth]{layout-test/mock_results_stacked.pdf}
  \end{tikzfigure}
}
```

After compiling, check the rendered PDF: does the bottom of Band 3
(Acknowledgements) still sit above the 20 mm bottom `innermargin`? If it
overflows, the Methodology schematic in Band 1 (currently `width=0.48\linewidth`,
a tall portrait image) is the thing to shrink first.
